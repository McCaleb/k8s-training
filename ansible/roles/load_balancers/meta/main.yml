---
# load_balancers: role metadata

galaxy_info:
  author: Luke McCaleb
  description: HAProxy and keepalived HA endpoint for the Kubernetes API server
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - kubernetes
    - haproxy
    - keepalived
    - vrrp

# Ordering is handled explicitly in playbooks/site.yml. Declaring roles here
# would execute them again, per role, on every host.
dependencies: []
...
