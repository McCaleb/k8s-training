---
# debian_update: role metadata

galaxy_info:
  author: Luke McCaleb
  description: Patches Debian hosts and handles kernel reboots via needrestart
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - debian
    - apt
    - needrestart
    - patching

# Ordering is handled explicitly in playbooks/site.yml. Declaring roles here
# would execute them again, per role, on every host.
dependencies: []
...
