---
# common: role metadata

galaxy_info:
  author: Luke McCaleb
  description: Baseline configuration and Kubernetes node prep for Debian cluster nodes
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - kubernetes
    - debian
    - chrony
    - containerd

# Ordering is handled explicitly in playbooks/site.yml. Declaring roles here
# would execute them again, per role, on every host.
dependencies: []
...
