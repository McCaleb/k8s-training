---
# worker_nodes: role metadata

galaxy_info:
  author: Luke McCaleb
  description: Joins worker nodes to a kubeadm cluster
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - kubernetes
    - kubeadm

# Ordering is handled explicitly in playbooks/site.yml. Declaring roles here
# would execute them again, per role, on every host.
dependencies: []
...
